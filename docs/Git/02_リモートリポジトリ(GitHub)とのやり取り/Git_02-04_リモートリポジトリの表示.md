# リモートリポジトリの表示

## リモートリポジトリを表示する

コマンド
```
git remote

# 対応するURLを表示する
git remote -v
```

実行例
```
>git remote
origin

>git remote -v
origin  https://github.com/lm14adgp/test.git (fetch)
origin  https://github.com/lm14adgp/test.git (push)

>
```
